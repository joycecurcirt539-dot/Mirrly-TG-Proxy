Переход на собственный Rust-движок MirrlyEngine, поддержка SOCKS5 и отказ от TCP-фоллбека.

---
## 1. Контрольные отпечатки SHA-256
* **app-universal-release.apk SHA-256**: `D4971BE6B4070019E316FBBAC4456AAEB1649833B5A3073E6DBF23AB5892BBE3`
* **app-arm64-v8a-release.apk SHA-256**: `5D0B69B465D5DFA87F4230D8701B85374627239D5AB70B5A3B5D95307CEF6394`
* **app-armeabi-v7a-release.apk SHA-256**: `6F70C0D48712AA9D263B454C4D78621F1EB57691835BF586CB9BF88B8CDCD5B7`
* **app-x86_64-release.apk SHA-256**: `BDA03264FC16EC4C762CFAC2A962299704206E7C797C5CC8244A0D2FAA41F80E`
* **app-x86-release.apk SHA-256**: `8D7500696F489A81634D8202BBF611F2022E4F86DF6D1A905ED5B700312368C3`
* **SHA-256 подписи (Release)**: `97:73:5C:0A:20:70:7F:D4:E4:BD:93:A2:D8:48:CA:91:9A:C5:40:45:4A:62:16:E8:CC:7D:43:4F:1F:9F:0A:96`

---
## 2. Назначение установочных пакетов (APK)
* **app-universal-release.apk**: Универсальная сборка. Содержит нативные библиотеки под все архитектуры (ARM64, ARMv7, x86, x86_64). Гарантированно подходит для любого Android-устройства. Единый основной файл для автообновлений и публикации.
* **app-arm64-v8a-release.apk**: Сборка ARM64. Оптимизирована для большинства современных смартфонов и планшетов (64-битные процессоры ARM). Обладает меньшим размером.
* **app-armeabi-v7a-release.apk**: Сборка ARMv7. Для старых 32-битных Android-устройств.
* **app-x86_64-release.apk**: Сборка x86_64. Для 64-битных эмуляторов Android (Android Studio, LDPlayer, BlueStacks) и устройств на базах процессоров Intel/AMD.
* **app-x86-release.apk**: Сборка x86. Для 32-битных эмуляторов Android x86.

---
## 3. Ключевые изменения

### Собственное ядро и производительность
* **Переход на собственный нативный движок MirrlyEngine**:
  * Полный отказ от сторонней устаревшей Go-библиотеки в пользу собственного высокопроизводительного асинхронного движка `mirrlyengine` на базе Rust и Tokio.
  * Реализован умный пул предварительно прогретых WSS-сокетов (`WsPool`) с нулевой задержкой отклика (0 мс) при открытии чатов и загрузке медиа.
  * Точный фрейминг MTProto пакетов (Abridged, Intermediate, Padded) для стабильной передачи данных без разрывов сессий.
  * Интеграция быстрого DNS-over-HTTPS (DoH) резолвера с параллельной гонкой запросов (Cloudflare, Google, Quad9, AdGuard).
  * Нативные бинарные сборки скомпилированы под все ключевые архитектуры: `arm64-v8a`, `armeabi-v7a`, `x86_64`, `x86`.

### Сетевой стек и протоколы
* **Поддержка SOCKS5 прокси**:
  * Реализован нативный высокоскоростной TCP-релей SOCKS5 на локальном порту `10808` для передачи трафика чатов, медиафайлов и аудио/видеозвонков через WebSocket.
* **Исключение прямого TCP-фоллбека**:
  * Полностью удален механизм прямого TCP-подключения к дата-центрам Telegram в обход CDN. Трафик направляется исключительно через защищенный Anycast туннель Cloudflare.
* **Удаление воркера разработчика**:
  * Полностью вырезаны все обращения к стороннему воркеру разработчика.
  * Трафик распределяется по Anycast узлам с сохранением приоритета пользовательского кастомного домена в настройках.

### Интерфейс и дизайн
* **Обновленный визуальный стиль диалогов**:
  * Переработаны системные диалоговые окна: применен прозрачный стиль Liquid Glass с размытием фона, тонкими границами и четкими контурами элементов.
  * Улучшена отзывчивость интерфейса главного экрана и графиков активности сокетов.
